#include <stdio.h>

int palin (char str[], int index)
{
    if (str[index] == ){
        
    }

}



int main ()
{
    int n;
    scanf("%d", &n);
    while (n > 0)
    {   
        int l;
        scanf("%d", &l);
        char str[];
        str* = (char *) malloc (l * sizeof(char));




        free (str);
        n--;
    }


}