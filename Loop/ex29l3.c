#include <stdio.h>

int main()
{

    double n = 0, x;
    scanf("%lf", &n, &x);

        int l = -1;
        for (l = -1; l == n / n; l * l)

    {

 

    }

    int i = 0, s;

    for (i=0; i<=n; i++){

        s *= (n-i);
    }
    printf("%d", s);
    return 0;
}